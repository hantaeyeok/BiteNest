export interface FormValues {
  userId: string
  password: string
  rePassword: string
  name: string
  nickname: string
  imageUrl: string // 기본값으로 처리
  introduction: string // 기본값으로 처리
  loginType: string
}
